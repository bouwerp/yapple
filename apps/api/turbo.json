{
  "globalEnv": [
    "JWT_SECRET",
    "PASSWORD_SALT_ROUNDS",
    "ADMIN_USERS"
  ],
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "env": [
        "PORT"
      ],
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**"
      ]
    }
  }
}